name: CI

on:
  push:
    branches:
      - main
  workflow_run:
    workflows:
      - Upgrade
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  checks: write

env:
  CI: true
  NODE_ENV: development

defaults:
  run:
    shell: bash

jobs:
  test:
    name: CI for Node.js ${{ matrix.node }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/.cache/ms-playwright
    strategy:
      fail-fast: false
      matrix:
        node:
          - 20
          - 22
          - 24
          - 25
        os:
          - ubuntu-24.04
          - macos-26
          - windows-2025
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
          version: latest

      - name: Setup node ${{ matrix.node }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          check-latest: true
          cache: pnpm

      - name: Set Engine strict false for Node.js ${{ matrix.node }}
        run: pnpm config set --location=project engineStrict false

      - name: Install dependencies
        run: pnpm install

      - name: Try format
        run: pnpm run format

      - name: Try lint
        run: pnpm run lint

      - name: Try typecheck
        run: pnpm run typecheck

      - name: Try build
        env:
          NODE_ENV: production
        run: pnpm run build

      - name: Get Playwright version
        run: echo "PLAYWRIGHT_VERSION=$(pnpm exec playwright --version | awk '{print $2}')" >> $GITHUB_ENV

      - name: Cache Playwright Browsers
        uses: actions/cache@v5
        with:
          path: ${{ env.PLAYWRIGHT_BROWSERS_PATH }}
          key: ${{ runner.os }}-playwright-${{ env.PLAYWRIGHT_VERSION }}

      - name: Setup test environment
        run: pnpm run test:setup

      - name: Run tests
        run: pnpm run test --reporter=junit --outputFile=./reports/jest-junit.xml --reporter=hanging-process

      - name: Test Report
        uses: dorny/test-reporter@v3
        if: success() || failure()
        with:
          name: Tests for Node.js ${{ matrix.node }} on ${{ matrix.os }}
          path: reports/jest-*.xml
          reporter: jest-junit
